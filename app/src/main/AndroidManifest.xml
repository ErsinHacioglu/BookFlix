<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/bookflixlogored_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bookflixlogored_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookFlix"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <activity
            android:name="com.example.bookapp.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.bookapp.activities.LoginActivity" />
        <activity android:name="com.example.bookapp.activities.RegisterActivity" />
        <activity android:name="com.example.bookapp.activities.ProfileActivity" />
        <activity android:name="com.example.bookapp.activities.SubscriptionActivity" />
        <activity android:name="com.example.bookapp.activities.BookDetailActivity" />

    </application>

</manifest>
